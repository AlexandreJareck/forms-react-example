import styled from 'styled-components'

import { Container as CartContainerStyles } from 'components/shared/CardList/styles'

export const Container = styled.main`
  ${CartContainerStyles} {
    width: 56rem;
  }
`
